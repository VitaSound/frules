# Installed rule artifacts (regenerated by install.sh)
.cursor/rules/

# Local user overrides
frules.local.conf

# Source materials that should not be committed (size / license)
sources/*.pdf
sources/*.epub
sources/*.djvu
sources/*.mobi

# Editors
*.swp
*~
.DS_Store

# Local scratch (e.g. downloaded tool binaries for sandboxed runs)
.scratch/

# ML training artifacts
output/
.venv-train/
scripts/__pycache__/
data/raw.jsonl
data/augmented.jsonl
data/eval.jsonl
data/challenge-train.jsonl
