# rules/ — AI instruction modules (English only)

Each `*.mdc` is one topic. Bodies must be **English**.

Activation is configured in YAML frontmatter (`alwaysApply`, `globs`, `description`). See `docs/RULES-ARCHITECTURE.md`.

Do not put book prose here — distill actionable patterns and examples.
